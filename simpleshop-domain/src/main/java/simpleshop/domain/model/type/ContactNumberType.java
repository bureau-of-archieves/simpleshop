package simpleshop.domain.model.type;

/**
 * A list of default contact numbers which can be selected in the UI.
 */
public enum ContactNumberType {

    HOME_PHONE, WORK_PHONE, FAX, QQ, EMAIL, WORK_EMAIL
}
